// See https://umijs.org/docs/api/config for more about umi configurations.

import config from '@sigma-wg/umi-config/config.dev';
import { defineConfig } from 'umi';

export default defineConfig({
  ...config,
});
